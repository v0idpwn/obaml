{
  "name": "obaml",
  "version": "0.1.0",
  "description": "OCaml client for Oban",
  "license": "Apache 2.0",
  "esy": {
    "build": "dune build -p #{self.name}",
    "release": {
      "bin": "obaml",
      "includePackages": [
        "root",
        "@opam/camomile"
      ]
    }
  },
  "scripts": {
    "test": "esy x obaml"
  },
  "dependencies": {
    "@opam/dune": "*",
    "@opam/base": ">= 0.14.1",
    "@opam/core": ">= 0.14.1",
    "@opam/core_kernel": ">= 0.14.1",
    "ocaml": ">= 4.12.0"
  },
  "devDependencies": {
    "@opam/merlin": "4.2-412",
    "ocaml": "4.12.0"
  }
}
